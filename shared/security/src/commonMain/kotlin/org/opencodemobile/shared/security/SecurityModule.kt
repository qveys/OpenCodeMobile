package org.opencodemobile.shared.security

// SecureStore, HTTP policy, and biometrics ports (§7). Platform actuals (Keystore/Keychain) land in androidMain/iosMain here, never in a feature module.
public object SecurityModule
