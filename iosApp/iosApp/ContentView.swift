import SwiftUI

// Real navigation host wiring the features/* Compose screens together lands with the
// connection/projects/sessions feature work; this placeholder just proves the shell links.
struct ContentView: View {
    var body: some View {
        Text("OpenCode Mobile")
    }
}
